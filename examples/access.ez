// Access control demonstration (work in progress)
env native;

// Note: Class support is planned but not yet fully implemented
// This example shows the intended syntax for future use

/*
class Demo() { 
    public int add1(int x) { 
        return x + 1; 
    }
    
    private int secret(int x) { 
        return x * 2;
    }
}
*/

// For now, use regular functions
int add1(int x) {
    return x + 1;
}

// Test the function
printf(add1(5));


