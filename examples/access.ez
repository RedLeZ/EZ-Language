// Access control demonstration with comments
env native; // environment must be native

class Demo() { 
    /* class with public + private methods */
    public int add1(int x) { x + 1; } // public increment
    private int secret(int x) { x * 2; } // private double
}

// Top-level private variable (should raise diagnostic)
int y = 1; // invalid modifier at top level

// Calling public method (will not execute body in interpreter yet)


// Calling private method (should raise diagnostic)


