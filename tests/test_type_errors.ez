env native;

// This file contains code that should produce type errors

// Error 1: assigning wrong type to variable
// int x = true;  // should error: boolean to int

// Error 2: function call with wrong argument count
int add(int a, int b) {
    return a + b;
}

// add(5);  // should error: expects 2 args, got 1

// Error 3: wrong argument type
// add(5, true);  // should error: bool to int

// Error 4: arithmetic on booleans
// boolean x = true;
// x + 5;  // should error: arithmetic on non-int

// Error 5: function missing return
// int getValue() {
//     int x = 5;
// }  // should error: missing return

// Error 6: return value type mismatch
// boolean test() {
//     return 42;  // should error: int to boolean
// }

// Valid code that should work
int x = 5;
boolean flag = x > 3;
flag;
